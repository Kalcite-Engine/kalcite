# kalcite-runtime-core

`no_std` bounded-memory primitives shared by generated Kalcite targets. The first primitives are typed generational `Handle<T>` values and allocation-free `StaticPool<T, N>` storage.
