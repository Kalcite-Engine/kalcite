# Kalcite for VS Code

Support de `.klc` : coloration, accolades, commentaires et tâches. Installez la CLI `kalcite` dans le `PATH`, puis utilisez `kalcite lint`, `kalcite check` ou `kalcite build` depuis le terminal intégré.

Pour empaqueter : `npx @vscode/vsce package`.
