# kalcite-lsp
Protocol design: stdio LSP server for KLC/kscn diagnostics, hover, completion, go-to-definition and symbols. The semantic service is shared with kalcite-typecheck so VS Code and Zed receive the same file/line/column diagnostics as the CLI.
