@autoload
public class Game extend Node {
    u16 score = 0;
    public signal score_changed(u16 value);

    public void add_score(u8 value) {
        score += value;
        score_changed.emit(score);
    }
}
