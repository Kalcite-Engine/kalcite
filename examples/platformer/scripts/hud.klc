@component
public class Hud extend Node {
    @export
    public Player player;

    public void Ready() {
        player.coin_collected.connect(on_coin_collected);
    }

    public void on_coin_collected(u8 value) {
        Game.add_score(value);
    }
}
