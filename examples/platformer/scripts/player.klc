@component
@pool(1)
public class Player extend Node2D {
    @export
    fx8 speed = 2;

    public signal coin_collected(u8 value);

    public void Update() {
        position.x += Input.axis(Key.Left, Key.Right) * speed;
    }
}
