# Kalcite Engine v0.12

Scene compiler, static signals/autoload metadata, RGB565/RLE asset primitives, sprites/tilemaps, camera/layers, deterministic collisions, input actions, audio API, typed-save migration API, semantic diagnostics foundation, KLC test runner, profiler, package lock model, and MIR folding/narrowing.
